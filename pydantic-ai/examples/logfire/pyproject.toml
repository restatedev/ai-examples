[project]
name = "agents"
version = "0.1.0"
description = "Python template using Restate with Pydantic AI"
requires-python = ">=3.13"

dependencies = [
    "hypercorn",
    "pydantic-ai>=1.68.0",
    "restate-sdk>=0.17.1",
]

[dependency-groups]
dev = [
    "mypy>=1.18.2",
]
