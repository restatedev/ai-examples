[project]
name = "agents"
version = "0.1.0"
description = "Python examples using Restate with OpenAI Agent SDK"
requires-python = ">=3.12"

dependencies = [
    "hypercorn",
    "restate_sdk[serde]>=0.7.2",
    "pydantic>=2.10.6",
    "openai-agents==0.0.17",
]